---
phase: 02-component-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/layouts/Base.astro
autonomous: true
---

<objective>
## Goal
Establish the full design token system and base styles from the Document view reference, and fix the Google Fonts import in Base.astro to match the correct typefaces.

## Purpose
Every component in Plans 02-02 through 02-04 depends on these CSS custom properties being available. Getting the token names and font stack right now means components can reference `var(--ink)`, `var(--accent)` etc. without rework. Also corrects the font mismatch introduced in 01-01 (Instrument Serif was loaded; the reference uses IBM Plex Serif + Fraunces).

## Output
- `src/styles/global.css` — CSS custom properties, reset, and base element styles
- `src/layouts/Base.astro` — updated Google Fonts URL, global.css import
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Source Files
@PP Personal website/Personal Website.html
@src/layouts/Base.astro
</context>

<acceptance_criteria>

## AC-1: Design tokens available globally
```gherkin
Given the site is built
When any page is loaded in a browser
Then CSS custom properties (--paper, --ink, --accent, --mono, --serif, etc.) resolve correctly on :root
```

## AC-2: Correct fonts load
```gherkin
Given the site is built
When the browser loads the page
Then Inter, JetBrains Mono, IBM Plex Serif, and Fraunces are requested from Google Fonts (not Instrument Serif)
```

## AC-3: Build and tests still pass
```gherkin
Given global.css is imported and Base.astro is updated
When `npm run build` and `npx playwright test` are run
Then both exit 0 with no errors
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create global.css with design tokens and base reset</name>
  <files>src/styles/global.css</files>
  <action>
    Create `src/styles/global.css` with exactly these contents, taken from the Document view of `PP Personal website/Personal Website.html`:

    **:root — design tokens:**
    ```css
    :root {
      --paper: #eceef1;
      --paper-2: #e1e4e8;
      --ink: #141416;
      --ink-2: #2a2a2d;
      --ink-muted: #6b6b72;
      --rule: rgba(20, 20, 22, .15);
      --accent: #d94a28;
      --body: 'Inter', system-ui, sans-serif;
      --mono: 'JetBrains Mono', ui-monospace, monospace;
      --serif: 'IBM Plex Serif', 'Fraunces', Georgia, serif;
    }
    ```

    **Reset and base element styles** (also from the reference):
    ```css
    *, *::before, *::after { box-sizing: border-box; }
    html, body {
      margin: 0;
      padding: 0;
      background: var(--paper);
      color: var(--ink);
      font-family: var(--body);
      -webkit-font-smoothing: antialiased;
    }
    a { color: inherit; }
    ```

    Do NOT add any component-level styles — global.css is token/reset only. Components get their own scoped styles in Plans 02-02 through 02-04.
  </action>
  <verify>File exists at `src/styles/global.css`; contains `--accent: #d94a28` and `--serif: 'IBM Plex Serif'`</verify>
  <done>AC-1 satisfied: design tokens defined at :root</done>
</task>

<task type="auto">
  <name>Task 2: Update Base.astro — fix fonts, import global.css</name>
  <files>src/layouts/Base.astro</files>
  <action>
    Two changes to `src/layouts/Base.astro`:

    **1. Replace the Google Fonts `<link>` href** with the correct URL from `Personal Website.html`:
    ```
    https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;700&family=Inter:wght@300;400;500;600;700&family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=IBM+Plex+Serif:ital,wght@0,400;0,500;0,600;1,400;1,500&display=swap
    ```
    (Removes Instrument Serif; adds IBM Plex Serif and Fraunces.)

    **2. Add a `<style>` import for global.css** in the frontmatter using Astro's import syntax:
    ```ts
    import '../styles/global.css';
    ```
    Place it after the existing Props interface in the `---` frontmatter block.

    Keep all other Base.astro content unchanged.
  </action>
  <verify>`npm run build` exits 0; inspect `dist/index.html` — Google Fonts link href contains "IBM+Plex+Serif" and does NOT contain "Instrument+Serif"</verify>
  <done>AC-2 and AC-3 satisfied: correct fonts loaded, build passes</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/pages/index.astro` — placeholder page untouched
- `tests/smoke.spec.ts` — smoke tests unchanged (still pass against placeholder)
- `.github/workflows/deploy.yml` — CI pipeline unchanged
- `playwright.config.ts` — test config unchanged

## SCOPE LIMITS
- No component styles — scoped CSS lives in each component (Plans 02-02 through 02-04)
- No layout structure changes — Base.astro shell stays minimal
- No content or data files — Phase 2 content layer is Plans 02-03 through 02-05

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `src/styles/global.css` exists with all tokens and reset
- [ ] `npm run build` exits 0
- [ ] `dist/index.html` contains `IBM+Plex+Serif` in the Google Fonts href
- [ ] `dist/index.html` does NOT contain `Instrument+Serif`
- [ ] `npx playwright test` — 2/2 pass
</verification>

<success_criteria>
- Both tasks complete
- All verification checks pass
- AC-1, AC-2, AC-3 satisfied
</success_criteria>

<output>
After completion, create `.paul/phases/02-component-build/02-01-SUMMARY.md`
</output>
