---
phase: 02-component-build
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - src/data/site.json
  - src/pages/index.astro
  - src/components/Nav.astro
  - src/components/Hero.astro
  - src/components/Contact.astro
autonomous: true
---

<objective>
## Goal
Extract every remaining hardcoded text value across all components into a single `src/data/site.json` config file — after this plan, no copy, contact info, logo path, or structural text requires touching a component or template file.

## Purpose
Completes the "content editable without code" requirement. Work items and experience entries are already in YAML collections; this plan covers the site identity, hero copy, and contact metadata that was still hardcoded in component markup.

## Output
- `src/data/site.json` — single source of truth for: name, page title, logo path, email, phone, copyright, hero dateline + lede, contact stack + languages + base
- `src/pages/index.astro` — page title read from site.json
- `src/components/Nav.astro` — name and logo path read from site.json
- `src/components/Hero.astro` — all hero copy read from site.json
- `src/components/Contact.astro` — stack, languages, base, copyright, logo read from site.json
</objective>

<context>
@.paul/PROJECT.md
@src/components/Nav.astro
@src/components/Hero.astro
@src/components/Contact.astro
@src/pages/index.astro
</context>

<acceptance_criteria>

## AC-1: All text configurable via site.json
```gherkin
Given src/data/site.json exists with all identity and copy fields
When a user edits any field (name, lede, stack lines, etc.) and runs npm run build
Then dist/index.html reflects the change — no component file needs editing
```

## AC-2: Build and smoke tests pass
```gherkin
Given all components import from site.json
When `npm run build` and `npx playwright test` are run
Then both exit 0 with no regressions
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create site.json</name>
  <files>src/data/site.json</files>
  <action>
    Create `src/data/site.json` with all site-wide config:
    - `name`: full name (used in Nav, Hero h1, byline)
    - `pageTitle`: browser tab title
    - `logo`: path to logo asset
    - `email`, `phone`: contact details
    - `copyright`: signature line
    - `hero.dateline`: array of 3 dateline spans
    - `hero.lede`: career summary paragraph
    - `contact.stack`: array of stack lines (rendered with br between)
    - `contact.languages`: array of language lines
    - `contact.base`: location string
  </action>
  <verify>File exists and is valid JSON (`node -e "require('./src/data/site.json')"` exits 0)</verify>
  <done>Source of truth created; all fields present</done>
</task>

<task type="auto">
  <name>Task 2: Wire all components to site.json</name>
  <files>src/pages/index.astro, src/components/Nav.astro, src/components/Hero.astro, src/components/Contact.astro</files>
  <action>
    Each file imports `site` from `'../data/site.json'` (adjust relative path per component depth) and replaces every hardcoded value with the corresponding `site.*` reference.
    Markup changes only — scoped styles in every component stay untouched.
  </action>
  <verify>`npm run build` exits 0; `npx playwright test` 2/2 pass</verify>
  <done>AC-1 and AC-2 satisfied</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- All scoped `<style>` blocks — visual styles unchanged
- `src/content/` — YAML data files unchanged
- `src/data/links.json` — social links file unchanged
- `.github/workflows/deploy.yml` — CI pipeline unchanged

## SCOPE LIMITS
- Nav links (Index/Work/Experience/Contact) stay hardcoded — they map to section id attributes, not copy
- No new npm dependencies
</boundaries>

<verification>
- [ ] `src/data/site.json` exists with all fields
- [ ] `npm run build` exits 0
- [ ] `dist/index.html` contains page title, name, lede from site.json
- [ ] `npx playwright test` 2/2 pass
</verification>

<output>
After completion, create `.paul/phases/02-component-build/02-06-SUMMARY.md`
</output>
