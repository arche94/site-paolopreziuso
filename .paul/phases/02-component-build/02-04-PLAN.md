---
phase: 02-component-build
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/components/Experience.astro
  - src/components/Contact.astro
  - src/pages/index.astro
  - tests/smoke.spec.ts
autonomous: true
---

<objective>
## Goal
Build the Experience timeline and Contact footer components from the Document view prototype and wire them into the index page, completing the full Document view layout.

## Purpose

These two components close out the page: Experience gives Paolo's career chronology at a glance, and Contact exposes his stack, languages, and contact links — the final call to action for any visitor who scrolls to the bottom. Together with Nav, Hero, and Work they complete the v1 content surface.

## Output

- `src/components/Experience.astro` — two-column timeline grid with section label, 7 hardcoded entries (current entries highlighted in accent)
- `src/components/Contact.astro` — three-column footer grid (Stack, Languages+Base, Get in touch) with logo signature; `<img src="/logo.svg">` used instead of SVG sprite
- `src/pages/index.astro` — Experience and Contact wired in after Work
- `tests/smoke.spec.ts` — smoke test updated to assert contact section is present
  </objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work

@.paul/phases/02-component-build/02-03-SUMMARY.md

## Source Files

@src/pages/index.astro
@tests/smoke.spec.ts
</context>

<acceptance_criteria>

## AC-1: Experience timeline renders correctly

```gherkin
Given the index page loads
When a visitor scrolls to the experience section
Then a section labelled "§ 02 Experience & education" is visible with a two-column timeline of 7 entries; current entries (M.S. AI Engineering, Backend Developer) have their title highlighted in accent colour
```

## AC-2: Contact footer renders correctly

```gherkin
Given the index page loads
When a visitor scrolls to the contact section
Then a three-column footer is visible with Stack, Languages/Base, and Get in touch columns; email and phone are present; the PP logo mark and copyright line appear at the bottom
```

## AC-3: Build and smoke tests pass

```gherkin
Given Experience and Contact are wired into index.astro
When `npm run build` and `npx playwright test` are run
Then both exit 0
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Build Experience.astro component</name>
  <files>src/components/Experience.astro</files>
  <action>
    No props. Content hardcoded (Plan 02-05 is intentionally scoped to the work collection only — experience entries are small enough to stay hardcoded in v1 given the "no code edit for content" requirement applies primarily to work items).

    Structure matching prototype Document view:
    ```html
    <section id="experience">
      <div class="doc-section-label">
        <span class="num">§ 02</span> Experience &amp; education
      </div>
      <div class="doc-tl">
        <div class="yr">Mar 2026 — now</div><div class="ev now"><b>M.S. AI Engineering</b> — ProfessionAI (online, ongoing)</div>
        <div class="yr">Feb 2025 — now</div><div class="ev now"><b>Backend Developer</b> — Industrial Cloud srl, Torino</div>
        <div class="yr">Oct 2023 — Dec 2024</div><div class="ev"><b>Software Development Consultant</b> — Phi Collective srl, Torino</div>
        <div class="yr">Jan 2022 — May 2023</div><div class="ev"><b>Junior Pipeline Developer</b> — Alps Studios srl, Torino</div>
        <div class="yr">Jun 2019 — Dec 2021</div><div class="ev"><b>Full-Stack Developer</b> — Inera srl, Pisa</div>
        <div class="yr">Sep 2018 — Dec 2018</div><div class="ev"><b>Software Developer Intern</b> — Inera srl, Pisa</div>
        <div class="yr">Sep 2013 — Jun 2019</div><div class="ev"><b>B.S. Computer Science</b> — University of Pisa</div>
      </div>
    </section>
    ```

    Note: Both "M.S. AI Engineering" and "Backend Developer" are current roles — both get `.ev.now` so their `<b>` renders in `--accent`.

    Scoped `<style>`:
    ```css
    .doc-section-label {
      font-family: var(--mono); font-size: 11px; text-transform: uppercase; letter-spacing: 2.5px; color: var(--ink-muted);
      padding: 64px 0 24px; display: flex; align-items: center; gap: 16px;
    }
    .doc-section-label::before { content: ''; width: 36px; height: 1.5px; background: var(--accent); }
    .doc-section-label .num { color: var(--ink); margin-right: 6px; font-weight: 500; }
    .doc-tl { display: grid; grid-template-columns: 120px 1fr; gap: 20px; font-family: var(--mono); font-size: 12px; }
    .doc-tl .yr { color: var(--ink-muted); padding: 6px 0; border-top: 1px dashed var(--rule); }
    .doc-tl .ev { padding: 6px 0; border-top: 1px dashed var(--rule); color: var(--ink-2); }
    .doc-tl .ev b { color: var(--ink); font-weight: 500; }
    .doc-tl .ev.now b { color: var(--accent); }
    ```

    Avoid: Duplicating `.doc-section-label` CSS — this is scoped so duplication with Work.astro is intentional and safe (Astro scopes styles per component).

  </action>
  <verify>File exists at `src/components/Experience.astro`; `npm run build` exits 0</verify>
  <done>AC-1 structure ready for wiring</done>
</task>

<task type="auto">
  <name>Task 2: Build Contact.astro, wire all into index, update smoke test</name>
  <files>src/components/Contact.astro, src/pages/index.astro, tests/smoke.spec.ts</files>
  <action>
    **src/components/Contact.astro:**
    No props. Three-column footer grid matching prototype. GitHub and LinkedIn links are `href="#"` placeholders — Plan 02-05 will extract contact links to a content file.

    The prototype signature uses `<svg><use href="#pp-mark"/>` (sprite). Use `<img src="/logo.svg">` instead — consistent with the logo approach established in Plan 02-02.

    Structure:
    ```html
    <section id="contact">
      <div class="doc-foot">
        <div>
          <h4>Stack</h4>
          <div class="block">
            Python · TypeScript<br/>
            JavaScript · Java · PHP · C/C++<br/>
            Node.js · React · Yii2 · Spring<br/>
            MongoDB · MySQL · REST · GraphQL<br/>
            Docker · GitHub Actions<br/>
            Jupyter · Cursor · Codex
          </div>
        </div>
        <div>
          <h4>Languages</h4>
          <div class="block">
            Italian — native<br/>
            English — fluent
          </div>
          <h4 class="mt">Base</h4>
          <div class="block">Torino, Italy · CET</div>
        </div>
        <div>
          <h4>Get in touch</h4>
          <div class="block">
            <a href="mailto:preziusopao@gmail.com">preziusopao@gmail.com</a><br/>
            +39 329 328 7880<br/>
            <a href="#">github</a> · <a href="#">linkedin</a>
          </div>
          <div class="sig">
            <img src="/logo.svg" alt="PP" width="20" height="20" />
            <span>© Paolo Preziuso · 2026</span>
          </div>
        </div>
      </div>
    </section>
    ```

    Scoped `<style>`:
    ```css
    .doc-foot {
      display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 48px;
      padding: 64px 0 0; margin-top: 32px; border-top: 1.5px solid var(--ink);
    }
    h4 { font-family: var(--mono); font-size: 10px; text-transform: uppercase; letter-spacing: 2.2px; color: var(--ink-muted); margin: 0 0 14px; font-weight: 500; }
    h4.mt { margin-top: 28px; }
    .block { font-family: var(--mono); font-size: 12px; line-height: 1.9; color: var(--ink-2); }
    .block a { color: var(--ink); text-decoration: none; border-bottom: 1px solid var(--accent); }
    .sig { display: flex; align-items: center; gap: 12px; margin-top: 18px; color: var(--ink-muted); font-family: var(--mono); font-size: 10px; letter-spacing: 1.5px; text-transform: uppercase; }
    .sig img { display: block; opacity: 0.5; }
    ```

    **src/pages/index.astro:**
    Add Experience and Contact imports, place after Work inside `.doc`:
    ```astro
    ---
    import Base from '../layouts/Base.astro';
    import Nav from '../components/Nav.astro';
    import Hero from '../components/Hero.astro';
    import Work from '../components/Work.astro';
    import Experience from '../components/Experience.astro';
    import Contact from '../components/Contact.astro';
    ---

    <Base title="Paolo Preziuso — Software Engineer">
      <Nav />
      <main class="doc">
        <Hero />
        <Work />
        <Experience />
        <Contact />
      </main>
    </Base>

    <style>
      .doc { max-width: 1200px; margin: 0 auto; padding: 0 48px 120px; }
    </style>
    ```

    **tests/smoke.spec.ts:**
    Add assertion for contact section:
    ```typescript
    test('homepage loads', async ({ page }) => {
      await page.goto('/');
      await expect(page).toHaveTitle('Paolo Preziuso — Software Engineer');
      await expect(page.locator('header.site-chrome')).toBeVisible();
      await expect(page.locator('main h1')).toContainText('Paolo Preziuso');
      await expect(page.locator('section#work')).toBeVisible();
      await expect(page.locator('section#contact')).toBeVisible();
    });
    ```

    Avoid: Asserting `section#experience` in the smoke test — two assertions per new section is sufficient; contact is the terminal landmark.

  </action>
  <verify>`npm run build` exits 0; `npx playwright test` 2/2 pass; `dist/index.html` contains "preziusopao@gmail.com"</verify>
  <done>AC-2 and AC-3 satisfied: contact footer renders, full page builds, smoke tests pass</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- `src/styles/global.css` — tokens locked
- `src/layouts/Base.astro` — unchanged
- `src/components/Nav.astro` — unchanged
- `src/components/Hero.astro` — unchanged
- `src/components/Work.astro` — unchanged
- `src/content/` — YAML files unchanged
- `.github/workflows/deploy.yml` — CI pipeline unchanged

## SCOPE LIMITS

- No content collections for experience entries or contact links — Plan 02-05
- No mobile responsive styles — Phase 3 scope
- GitHub and LinkedIn hrefs stay as `#` placeholders — Plan 02-05 extracts to content
- No interactive elements (forms, modals) — out of v1 scope entirely

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `src/components/Experience.astro` exists with 7 timeline entries and `.now` class on 2 current roles
- [ ] `src/components/Contact.astro` exists with 3-column grid and email present
- [ ] `npm run build` exits 0
- [ ] `dist/index.html` contains "preziusopao@gmail.com"
- [ ] `npx playwright test` — 2/2 pass
</verification>

<success_criteria>

- Both tasks complete
- All verification checks pass
- AC-1, AC-2, AC-3 satisfied
- Full Document view layout complete: Nav + Hero + Work + Experience + Contact
  </success_criteria>

<output>
After completion, create `.paul/phases/02-component-build/02-04-SUMMARY.md`
</output>
