---
phase: 02-component-build
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - src/content.config.ts
  - src/content/experience/01-ms-ai.yaml
  - src/content/experience/02-backend-dev.yaml
  - src/content/experience/03-dev-consultant.yaml
  - src/content/experience/04-pipeline-dev.yaml
  - src/content/experience/05-fullstack-dev.yaml
  - src/content/experience/06-intern.yaml
  - src/content/experience/07-bs-cs.yaml
  - src/components/Experience.astro
  - src/data/links.json
  - src/components/Contact.astro
  - tests/smoke.spec.ts
autonomous: true
---

<objective>
## Goal
Wire experience timeline entries and contact links to editable data files, making all variable content on the page code-free to edit.

## Purpose
This closes the Phase 2 content-editable requirement. After this plan, Paolo can update his timeline, GitHub/LinkedIn URLs, without touching any component code — matching the core project requirement.

## Output
- `src/content.config.ts` — experience collection added alongside work
- `src/content/experience/*.yaml` — 7 experience/education entries as editable YAML files
- `src/components/Experience.astro` — rewritten to fetch from collection
- `src/data/links.json` — GitHub and LinkedIn URLs as a simple JSON config
- `src/components/Contact.astro` — GitHub/LinkedIn hrefs read from links.json
- `tests/smoke.spec.ts` — smoke test updated to assert a contact link is present
</objective>

<context>
## Project Context
@.paul/PROJECT.md

## Prior Work
@.paul/phases/02-component-build/02-04-SUMMARY.md

## Source Files
@src/content.config.ts
@src/components/Experience.astro
@src/components/Contact.astro
@tests/smoke.spec.ts
</context>

<acceptance_criteria>

## AC-1: Experience entries editable via YAML
```gherkin
Given experience entries exist as YAML files in src/content/experience/
When a user edits a title or period in any YAML file and runs npm run build
Then dist/index.html reflects the change — no component code edit required
```

## AC-2: Contact links editable via JSON
```gherkin
Given GitHub and LinkedIn URLs are defined in src/data/links.json
When a user updates either URL in links.json and runs npm run build
Then the rendered page reflects the new href — no component code edit required
```

## AC-3: Build and smoke tests pass
```gherkin
Given all data wiring is complete
When `npm run build` and `npx playwright test` are run
Then both exit 0 — no regressions in nav, hero, work, experience, or contact
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add experience content collection and 7 YAML entries</name>
  <files>src/content.config.ts, src/content/experience/01-ms-ai.yaml, src/content/experience/02-backend-dev.yaml, src/content/experience/03-dev-consultant.yaml, src/content/experience/04-pipeline-dev.yaml, src/content/experience/05-fullstack-dev.yaml, src/content/experience/06-intern.yaml, src/content/experience/07-bs-cs.yaml, src/components/Experience.astro</files>
  <action>
    **src/content.config.ts** — add experience collection alongside the existing work collection:
    ```typescript
    const experience = defineCollection({
      loader: glob({ pattern: '**/*.yaml', base: './src/content/experience' }),
      schema: z.object({
        order: z.number(),
        period: z.string(),
        title: z.string(),
        company: z.string(),
        current: z.boolean().default(false),
      }),
    });

    export const collections = { work, experience };
    ```

    **YAML files** (newest-first order, `current: true` on active roles):

    `01-ms-ai.yaml`:
    ```yaml
    order: 1
    period: "Mar 2026 — now"
    title: "M.S. AI Engineering"
    company: "ProfessionAI (online, ongoing)"
    current: true
    ```

    `02-backend-dev.yaml`:
    ```yaml
    order: 2
    period: "Feb 2025 — now"
    title: "Backend Developer"
    company: "Industrial Cloud srl, Torino"
    current: true
    ```

    `03-dev-consultant.yaml`:
    ```yaml
    order: 3
    period: "Oct 2023 — Dec 2024"
    title: "Software Development Consultant"
    company: "Phi Collective srl, Torino"
    current: false
    ```

    `04-pipeline-dev.yaml`:
    ```yaml
    order: 4
    period: "Jan 2022 — May 2023"
    title: "Junior Pipeline Developer"
    company: "Alps Studios srl, Torino"
    current: false
    ```

    `05-fullstack-dev.yaml`:
    ```yaml
    order: 5
    period: "Jun 2019 — Dec 2021"
    title: "Full-Stack Developer"
    company: "Inera srl, Pisa"
    current: false
    ```

    `06-intern.yaml`:
    ```yaml
    order: 6
    period: "Sep 2018 — Dec 2018"
    title: "Software Developer Intern"
    company: "Inera srl, Pisa"
    current: false
    ```

    `07-bs-cs.yaml`:
    ```yaml
    order: 7
    period: "Sep 2013 — Jun 2019"
    title: "B.S. Computer Science"
    company: "University of Pisa"
    current: false
    ```

    **src/components/Experience.astro** — replace hardcoded rows with collection fetch:
    ```astro
    ---
    import { getCollection } from 'astro:content';
    const entries = (await getCollection('experience')).sort((a, b) => a.data.order - b.data.order);
    ---

    <section id="experience">
      <div class="doc-section-label">
        <span class="num">§ 02</span> Experience &amp; education
      </div>
      <div class="doc-tl">
        {entries.map(entry => (
          <>
            <div class="yr">{entry.data.period}</div>
            <div class={`ev${entry.data.current ? ' now' : ''}`}>
              <b>{entry.data.title}</b> — {entry.data.company}
            </div>
          </>
        ))}
      </div>
    </section>
    ```
    Keep the existing scoped `<style>` block unchanged.

    Avoid: Removing the scoped styles — they must remain in the component.
  </action>
  <verify>`npm run build` exits 0; `dist/index.html` contains "M.S. AI Engineering" rendered from YAML (not hardcoded)</verify>
  <done>AC-1 satisfied: experience entries live in editable YAML files</done>
</task>

<task type="auto">
  <name>Task 2: Wire contact links from JSON and update smoke test</name>
  <files>src/data/links.json, src/components/Contact.astro, tests/smoke.spec.ts</files>
  <action>
    **src/data/links.json** — simple JSON config; Paolo fills in real URLs before launch:
    ```json
    {
      "github": "https://github.com/paolopreziuso",
      "linkedin": "https://www.linkedin.com/in/paolopreziuso"
    }
    ```

    **src/components/Contact.astro** — add frontmatter to import links, replace `href="#"` anchors:
    ```astro
    ---
    import links from '../data/links.json';
    ---
    ```
    Then update the two anchor tags:
    ```html
    <a href={links.github}>github</a> · <a href={links.linkedin}>linkedin</a>
    ```
    Leave all other markup and styles unchanged.

    **tests/smoke.spec.ts** — add assertion that the github link in the contact section is present and has a real href (not `#`):
    ```typescript
    await expect(page.locator('section#contact a[href*="github"]')).toBeVisible();
    ```
    Add this assertion to the existing "homepage loads" test, after the `section#contact` check.

    Avoid: Importing links.json in the component without a frontmatter block — Astro requires imports inside `---` fences.
  </action>
  <verify>`npx playwright test` 2/2 pass; `dist/index.html` contains `href="https://github.com/paolopreziuso"`</verify>
  <done>AC-2 and AC-3 satisfied: contact links editable via JSON, all smoke tests pass</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/styles/global.css` — tokens locked
- `src/layouts/Base.astro` — unchanged
- `src/components/Nav.astro` — unchanged
- `src/components/Hero.astro` — unchanged
- `src/components/Work.astro` — unchanged
- `src/content/work/*.yaml` — existing work entries unchanged
- `.github/workflows/deploy.yml` — CI pipeline unchanged

## SCOPE LIMITS
- No changes to Experience scoped styles — only the frontmatter + markup is rewritten
- Stack list in Contact.astro stays hardcoded — it is not a content collection concern
- No new npm dependencies — JSON imported natively by Astro/Vite; no YAML plugin needed
- No Phase 3 work (responsive, SEO) in this plan

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `src/content.config.ts` exports both `work` and `experience` collections
- [ ] 7 YAML files exist under `src/content/experience/`
- [ ] `src/data/links.json` exists with github and linkedin keys
- [ ] `npm run build` exits 0
- [ ] `dist/index.html` contains "M.S. AI Engineering" and "https://github.com/paolopreziuso"
- [ ] `npx playwright test` — 2/2 pass
</verification>

<success_criteria>
- Both tasks complete
- All verification checks pass
- AC-1, AC-2, AC-3 satisfied
- All variable content on the page (work items, experience entries, contact links) is editable without touching component code — Phase 2 content requirement met
</success_criteria>

<output>
After completion, create `.paul/phases/02-component-build/02-05-SUMMARY.md`
</output>
